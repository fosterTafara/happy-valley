/* Mobile Menu */
/* The default is to hide the main menu and use a "hamburger" icon/button
   linked to some JavaScript to show/hide the main menu as needed. */

#main-menu {
    position: fixed; /* Position the menu at the top left of the window */
    top: 0;
    left: 0;
    height: 100%; /* The full height of the window */
    width: 0; /* 0 width - change this with JavaScript to show the menu */
    z-index: 10; /* Make the menu appear in front of other page content */
    overflow: hidden; /* Make sure the horizontal scrollbar does not appear inside the menu */
    padding-top: 2.75em; /* Make space for the close button - adjust value to suit the design */
    transition: 0.5s; /* 0.5 second transition effect to slide in the menu */ 

    /* Uncomment the next line and set a value if you do not want the menu to cover the full width of the viewport. */
    /* max-width: 30em; */    
}

#main-menu ul {
	white-space: nowrap;
}

#main-menu li {
    line-height: 1.2;
    border-bottom: 1px solid #fff;	
}

#main-menu a {
    display: block; /* Needs to be block, not inline (the default for <a>), so padding can be added */
    padding: 0.5em 0.5em 0.5em 2em;
    font-size: 1.25em;
    color: #000;
}

#menu-button, #close-button {
    /* Change the pointer to a hand if the user mouses over the buttons */
    cursor: pointer;
    line-height: 1;
}

#menu-button {
	/* Position the menu button at the top right of the header. If the menu
       button should not scroll with the page, change the position to fixed and
       adjust its top and right position as needed.*/
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    
    font-size: 1.8rem; /* The size of the hamburger icon */
}

#close-button {
    /* Position and style the close button at the top right corner of the 
       slide-out menu). Set the top and right values,  */
    position: absolute;
    top: 0;
    right: 0.3rem;
    
    font-size: 2.5rem; /* Change this so that the 'X' looks the same size as the hamburger icon */
    padding: 0;
}
