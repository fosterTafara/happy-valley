function openNav() {
    // Set the width of the main menu to 100% of its parent, which then shows the menu.
    document.getElementById("main-menu").style.width = "100%";
}

function closeNav() {
    // Set the width of the main menu to 0, which hides it.
    document.getElementById("main-menu").style.width = "0";
}